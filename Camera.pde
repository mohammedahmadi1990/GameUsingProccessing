/*
Put your projection and camera operations here.
Add more constants, variables and functions as needed.
*/

PMatrix3D projectOrtho, projectFrustum; // store two possible projection matrices

// prepare the orthographic and perspective projection matrices
void setupProjections() {
  /*
  You can use the versions of ortho() and perspective() that take parameters.  
  Remember to save the result of one before calling the next.
  Hint for ortho:  give the boundaries for the y direction in the order (top, bottom) to flip the y axis.
  Hint for perspective:  after perspective() or frustum(), call fixFrustumYFlip() to correct the y axis.
  */
  
  
}
